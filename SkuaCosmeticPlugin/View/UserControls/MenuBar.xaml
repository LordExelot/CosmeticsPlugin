<UserControl x:Class="Skua_CosmeticPlugin.View.UserControls.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Skua_CosmeticPlugin.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="44" d:DesignWidth="800"
             d:Background="#424242"
             d:Foreground="White"
             >

    <ListBox
        ItemsSource="{Binding TabItems, RelativeSource={RelativeSource AncestorType=Window}}"
        SelectedItem="{Binding SelectedTabItem, RelativeSource={RelativeSource AncestorType=Window}}"
        Style="{StaticResource MaterialDesignChoiceChipPrimaryOutlineListBox}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <TextBlock
                    materialDesign:RippleAssist.IsDisabled="True"
                    Style="{StaticResource MaterialDesignButtonTextBlock}"
                    Text="{Binding Header}"
                    TextAlignment="Center" />
            </DataTemplate>
        </ListBox.ItemTemplate>
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
    </ListBox>
</UserControl>
